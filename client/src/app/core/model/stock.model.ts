export interface IStockModel {
  id?: number,
  drawing?: number,
  location?: number,
  quantity?: number,
  description?: string;
  mdr?: number,
  partial?: boolean,
  status?: number,
  justify?: string
}
